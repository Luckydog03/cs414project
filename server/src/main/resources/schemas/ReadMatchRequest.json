{
    "$id": "https://example.com/readinvite.schema.json",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "readmatch",
    "description": "Read matches object for http request",
    "type": "object",
    "properties": {
        "requestType": {
            "description": "the object type should be readmatch",
            "type": "string",
            "pattern": "^readmatch$"
        },
        "receiver_id": {
            "type": "string",
            "description": "The receiver's user ID to retrieve matches for."
        }
    },
    "required": [
        "requestType",
        "receiver_id"
    ],
    "additionalProperties": false
}